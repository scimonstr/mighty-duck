language: java

jdk:
  - oraclejdk8

os:
  - linux

branches:
    - master
    - develop

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
  - $HOME/.gradle
  - $HOME/.gradle/wrapper/
